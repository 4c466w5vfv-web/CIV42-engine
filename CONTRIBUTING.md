# 기여 안내

CIV42 엔진은 Apache-2.0으로 공개됩니다. 포크·수정·상업적 사용 모두
자유입니다. 아래는 이 저장소에 기여를 보내실 때의 규칙입니다.

## 1. DCO — 기여자 원본 증명

CLA(저작권 양도 계약) 대신 **DCO(Developer Certificate of Origin)** 를
사용합니다. 서명은 커밋에 한 줄을 붙이는 것으로 끝납니다:

```bash
git commit -s -m "fix: ..."
```

이 명령이 커밋 메시지에 아래 줄을 자동으로 넣습니다.

```
Signed-off-by: 이름 <email@example.com>
```

이 서명은 "이 코드를 기여할 권리가 나에게 있다"는 진술입니다
(전문: https://developercertificate.org/). 서명이 없는 PR은 병합하지
않습니다 — 나중에 "내 기여분을 빼달라"는 분쟁을 막기 위해서입니다.

## 2. 판단이 바뀌는 변경은 근거와 함께

이 엔진은 사람의 결정에 쓰입니다. 다음을 바꾸는 PR은 **왜 그 값이 더
정확한지**를 함께 적어 주세요. 취향으로 바꾸면 되돌립니다.

- `ark42/ontology.py` 의 8개 기준, `ark42/disciplines.py` 의 20개 학문
- `ark42/prompts.py` 의 프롬프트 문구
- `ark42/montecarlo.py` 의 분포 가정·상관 구조
- `ark42/tensor.py` 의 가중치 산식

프롬프트를 바꾸면 `python3 tools/export_prompt_kit.py` 를 다시 실행해
`docs/PROMPT_KIT.md` 를 갱신하십시오. 갱신하지 않으면
`tests/test_prompt_kit.py` 가 실패합니다.

## 3. 정직성 규칙 (이 프로젝트의 핵심)

리뷰에서 가장 자주 되돌리는 유형입니다.

- **지어낸 정밀도 금지.** 근거 없는 확률·점수·비율을 만들지 않습니다.
  모르면 모른다고 출력하고, 불확실하면 표준편차와 낮은 confidence로
  표현합니다.
- **조용한 실패 금지.** 오류를 삼키고 그럴듯한 값을 반환하지 않습니다.
  실패는 크게 실패하고, 외부 API 오류는 응답 본문을 보존합니다.
- **성공한 척 금지.** 기능이 준비되지 않았으면 UNAVAILABLE로 표시합니다.
- **원가를 낮게 적지 않습니다.** 캐시·크레딧으로 아낀 비용도 실제 요율로
  계상합니다.

## 4. 테스트

```bash
python3 -m pytest tests/ -q
```

변경으로 새로 깨지는 테스트가 없어야 합니다. 새 동작에는 그 동작이
깨졌을 때 실패하는 테스트를 함께 넣어 주세요.

## 5. 상표

코드는 자유롭게 쓰셔도 됩니다. 다만 **"CIV42"라는 이름과 로고는 상표**이며
Apache-2.0은 상표권을 부여하지 않습니다(라이선스 6조). 포크를 CIV42라고
부르거나, 이 이름으로 인증·교육·자문을 판매하실 수는 없습니다. 자세한
경계는 `NOTICE` 를 보십시오.
